const Person = ({ person }) => {
    let myPerson

    switch (person.name) {
        case 'Luke Skywalker':
            myPerson = "LikeSkywalker.webp"
            break
    }
}

export default Person